# Dockerfile para o Admin Panel (React/Vite) - Modo Desenvolvimento
FROM node:20-alpine

# Definir diretório de trabalho
WORKDIR /app

# Copiar arquivos de dependências do admin
COPY admin/package*.json ./

# Instalar dependências
RUN npm ci

# Expor porta do Vite Admin
EXPOSE 3001

# Comando para iniciar o servidor de desenvolvimento
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3001"]

